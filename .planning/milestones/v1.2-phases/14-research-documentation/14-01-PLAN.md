---
phase: 14-research-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - research/optimization-writeup.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-03

must_haves:
  truths:
    - "optimization-writeup.md contains a section summarizing subwindow composition technique"
    - "optimization-writeup.md contains a section summarizing animation double-buffering technique"
    - "optimization-writeup.md contains a section summarizing modern guideline scoring pipeline"
    - "optimization-writeup.md contains a section summarizing hi-score file I/O via Darwin syscalls"
    - "optimization-writeup.md contains binary size analysis findings with section breakdown data"
    - "optimization-writeup.md contains per-file contribution data and optimization results"
  artifacts:
    - path: "research/optimization-writeup.md"
      provides: "Expanded research writeup with v1.1 techniques and binary analysis"
      contains: "## v1.1 Implementation Techniques"
  key_links:
    - from: "research/optimization-writeup.md"
      to: "research/binary-size-analysis.md"
      via: "Binary size data referenced and summarized"
      pattern: "section breakdown|per-file|77,016"
---

<objective>
Expand the existing optimization-writeup.md with v1.1 technique summaries and binary size analysis findings.

Purpose: The existing writeup covers v1.0 optimization techniques (NEON SIMD, register packing, binary stripping, etc.) but says nothing about the v1.1 features (subwindow composition, animation double-buffering, scoring pipeline, hi-score persistence) or the v1.2 binary size analysis. This plan adds both.

Output: Updated research/optimization-writeup.md with new sections for v1.1 techniques and binary analysis findings.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@research/optimization-writeup.md
@research/binary-size-analysis.md
@asm/render.s
@asm/animation.s
@asm/board.s
@asm/hiscore.s
@asm/layout.s
@asm/data.s
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add v1.1 technique summary sections to optimization-writeup.md</name>
  <files>research/optimization-writeup.md</files>
  <action>
Add a new top-level section "## v1.1 Implementation Techniques" to research/optimization-writeup.md, placed AFTER the existing "## ARM64-Specific vs Generic Optimizations" section and BEFORE "## Key Findings".

This section contains four subsections, each a SUMMARY (not deep dive -- the deep dive goes in 14-02's standalone document). Each subsection: 1 paragraph of what it is, 1 short code snippet (5-15 lines max) showing the key pattern, and 1 paragraph of why it matters. Target ~80-120 lines total for all four subsections.

### Subsection 1: "### 6. Subwindow Composition"
- Explain the ncurses subwindow hierarchy: 9 game windows created via newwin+derwin, stored as WINDOW* pointers in data.s
- Key pattern: the wnoutrefresh+doupdate protocol -- each window draws independently, a single doupdate flushes all changes to the terminal in one write() syscall
- Code example: show the _render_frame flow (werase containers, draw leaf windows, wnoutrefresh board after piece overlay, single doupdate)
- Layout geometry: 80x24 terminal split into leftmost(12w), middle_left(22w), middle_right(10w), rightmost(35w)
- Mention layout.s creates the hierarchy top-down, destroys bottom-up with NULL guards

### Subsection 2: "### 7. Animation Double-Buffering"
- Explain the 4 background animations (fire, water, snakes, Game of Life) using dispatch table + timer-gated updates
- Key pattern: fire uses buf1 (intensity) + buf2 (cooling map) as single-purpose buffers; water and life use buf1/buf2 as a true double-buffer pair with a swap flag toggled each frame
- Code example: show the water/life swap pattern (if swap=0: read buf1 write buf2; toggle swap flag)
- Mention: two 3,840-byte buffers (80*24*2 bytes per halfword cell) in __DATA for all animation state
- 4,664 bytes of animation code (20.3% of total __text)

### Subsection 3: "### 8. Modern Guideline Scoring Pipeline"
- Explain the NES/guideline scoring engine in _lock_piece (board.s)
- Pipeline: T-spin detection (3-corner rule on pivot) -> base_score lookup (normal table or tspin table) * level -> back-to-back 1.5x bonus -> combo bonus (50 * combo_count * level) -> perfect clear check
- Code example: show the b2b bonus calculation (the `add w10, w10, w10, lsr #1` pattern for 1.5x multiply without floating point)
- Score tables: _score_table (100/300/500/800 for 1-4 lines), _tspin_score_table (400/800/1200/1600 for 0-3 lines)
- Mention: all scoring state in __DATA (score, level, combo_count, b2b_active, is_tspin)

### Subsection 4: "### 9. Hi-Score File I/O via Darwin Syscalls"
- Explain direct Darwin ARM64 syscalls (open/read/write/close via svc #0x80) instead of C library wrappers
- Path resolution: getenv("HOME") + "/.yetris-hiscore" string concatenation on stack buffer
- File format: raw little-endian uint32 (4 bytes), no header, no text parsing
- Error handling: carry flag (b.cs) on syscall error -> bail silently, leaving _hiscore at current value
- Code example: show the syscall sequence (mov x16, #5; svc #0x80; b.cs Lbail)
- 352 bytes of code (2.1% of __text) -- smallest feature implementation in the project
  </action>
  <verify>
    <automated>grep -c "## v1.1 Implementation Techniques\|### 6\. Subwindow Composition\|### 7\. Animation Double-Buffering\|### 8\. Modern Guideline Scoring\|### 9\. Hi-Score File I/O" research/optimization-writeup.md | grep -q "^5$" && echo "PASS: all 5 headers present" || echo "FAIL: missing section headers"</automated>
  </verify>
  <done>optimization-writeup.md contains a "## v1.1 Implementation Techniques" section with 4 subsections (subwindow composition, animation double-buffering, scoring pipeline, hi-score file I/O), each with a code snippet and design rationale</done>
</task>

<task type="auto">
  <name>Task 2: Add binary size analysis findings to optimization-writeup.md</name>
  <files>research/optimization-writeup.md</files>
  <action>
Add a new section "## v1.2 Binary Size Analysis" to research/optimization-writeup.md, placed AFTER the new "## v1.1 Implementation Techniques" section and BEFORE "## Key Findings".

This section summarizes the key findings from research/binary-size-analysis.md (the detailed raw data stays in that file; this section is the narrative interpretation). Target ~60-80 lines.

Content to include:

1. **Section breakdown table** -- reproduce the segment summary table from binary-size-analysis.md:
   __TEXT (32 KB, 2 pages), __DATA_CONST (16 KB), __DATA (16 KB), __LINKEDIT (16 KB) = 77,016 bytes total.
   Mention actual content is only 39,267 bytes; rest is page alignment padding.

2. **Per-file contribution summary** -- top-3 files with percentages:
   render.s (27.9%, 4,664 bytes), animation.s (20.3%, 3,384 bytes), board.s (14.4%, 2,400 bytes).
   Note data.s is zero-code, all-data (10,423 bytes).
   Include the ASCII bar chart from binary-size-analysis.md.

3. **Binary growth narrative** -- from 55,672 bytes (v1.0) to 77,016 bytes (v1.2):
   +6,600 bytes __text (+65.4%), crossing the 16 KB page boundary requiring a second __TEXT page.
   Animation.s alone accounts for 51.3% of the __text growth.

4. **Optimization findings** -- NEON mask alignment reduction saved 12 bytes in __const, but page alignment absorbed the saving (file size unchanged). The binary's file size is fundamentally determined by segment count * page size. Only stripping (removing LINKEDIT symbols) provides meaningful file reduction: 77,016 -> 52,720 bytes (-31.5%).

5. **Cross-reference** to the detailed analysis: "See research/binary-size-analysis.md for complete per-file breakdown, LINKEDIT internals, symbol analysis, and optimization methodology."

Also update the existing "## Key Findings" section:
- Add finding #7: "v1.1 features added 6,600 bytes of code (+65.4%) while maintaining the same I/O-bound performance profile. Animation is the largest contributor, reflecting the computational cost of per-cell fire/water/life simulation vs the simpler game logic."
- Add finding #8: "Mach-O page alignment determines file size granularity. Small optimizations (12-byte alignment savings) are absorbed by 16 KB page boundaries. Only eliminating entire segments or stripping symbols changes the on-disk footprint."

Update the "## Project Overview" section near the top:
- Change the "~8,400 lines" count if needed (verify with `wc -l asm/*.s`)
- Update the source file count if needed (currently says "12 source files")

Update "## Conclusion" to mention v1.1 techniques and binary analysis as additional research findings, in 1-2 sentences.
  </action>
  <verify>
    <automated>grep -c "## v1.2 Binary Size Analysis\|Per-file\|Page alignment\|binary-size-analysis.md" research/optimization-writeup.md | grep -qE "^[4-9]" && echo "PASS: binary analysis section present with key content" || echo "FAIL: binary analysis section incomplete"</automated>
  </verify>
  <done>optimization-writeup.md contains a "## v1.2 Binary Size Analysis" section with section breakdown, per-file contributions, growth narrative, optimization findings, and cross-reference to the detailed analysis document. Key Findings and Conclusion sections are updated to reflect v1.1/v1.2 additions.</done>
</task>

</tasks>

<verification>
1. `grep "## v1.1 Implementation Techniques" research/optimization-writeup.md` returns a match
2. `grep "## v1.2 Binary Size Analysis" research/optimization-writeup.md` returns a match
3. `grep -c "### [6-9]\." research/optimization-writeup.md` returns 4 (four v1.1 subsections)
4. `grep "binary-size-analysis.md" research/optimization-writeup.md` returns a match (cross-reference)
5. `grep "finding.*[78]" research/optimization-writeup.md` or equivalent confirms new key findings added
6. The file has no broken markdown (no unclosed code blocks, no dangling headers)
</verification>

<success_criteria>
- optimization-writeup.md contains v1.1 technique summaries with code examples for all 4 techniques
- optimization-writeup.md contains binary size analysis findings with quantitative data
- Key Findings section includes new findings about v1.1 growth and Mach-O page alignment
- Conclusion section references v1.1 and v1.2 findings
- All data matches the source files and binary-size-analysis.md (no fabricated numbers)
</success_criteria>

<output>
After completion, create `.planning/phases/14-research-documentation/14-01-SUMMARY.md`
</output>
