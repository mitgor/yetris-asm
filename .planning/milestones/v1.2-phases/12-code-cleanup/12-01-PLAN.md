---
phase: 12-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - asm/board.s
  - asm/data.s
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "The binary builds with `make asm` and the game runs without regression after all removals"
    - "No function named _clear_lines exists in the codebase"
    - "All .globl symbols in data.s are referenced by at least one code path (direct or via pointer table)"
    - "No orphaned string literals remain in data.s"
  artifacts:
    - path: "asm/board.s"
      provides: "Board operations without dead _clear_lines function"
      contains: "_mark_lines"
    - path: "asm/data.s"
      provides: "Data section with only referenced symbols"
  key_links:
    - from: "asm/board.s"
      to: "asm/data.s"
      via: "_neon_row_mask still referenced by _mark_lines after _clear_lines removal"
      pattern: "_neon_row_mask@PAGE"
    - from: "asm/main.s"
      to: "asm/board.s"
      via: "_clear_marked_lines call unchanged"
      pattern: "bl.*_clear_marked_lines"
---

<objective>
Remove dead code and orphaned data from the assembly codebase.

Purpose: Eliminate the unused `_clear_lines` function (182 lines of dead code superseded by `_mark_lines` + `_clear_marked_lines` pipeline), remove orphaned string literals from data.s, and audit all `.globl` symbols to ensure every exported symbol is actually referenced. This reduces binary size and makes the codebase honest about what it contains.

Output: board.s without `_clear_lines`, data.s without orphaned strings, updated file headers/comments.
</objective>

<execution_context>
@/Users/mit/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mit/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@asm/board.s
@asm/data.s
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead _clear_lines function and update board.s</name>
  <files>asm/board.s</files>
  <action>
1. Delete the entire `_clear_lines` function (lines 591-772 approximately), including:
   - The comment block starting at `// _clear_lines() -> w0 = number of lines cleared (0-4)` (line 591)
   - The `.globl _clear_lines` directive (line 604)
   - The `.p2align 2` before it (line 605)
   - The function body from `_clear_lines:` through the `ret` at the end (lines 606-772)
   - All internal labels: `Lclear_row_loop`, `Lclear_done`, `Lclear_not_full`, `Lclear_shift_loop`, `Lclear_fill_top`, `Lclear_copy_byte`, `Lclear_zero_top`, `Lclear_level_scan`, `Lclear_level_done`, `Lstats_not_single`, `Lstats_not_double`, `Lstats_not_triple`, `Lstats_done`, `Lclear_return`

2. Update the file header comment block (lines 1-18):
   - Remove `_clear_lines` from the "Provides:" list (line 10)
   - Add `_mark_lines` and `_clear_marked_lines` to the Provides list if not already there
   - Update line 4 if it mentions "line clearing" to say "line marking and clearing"

3. Update stale comments in `_lock_piece` that reference `_clear_lines`:
   - Line 143: Change `// 4. Call _clear_lines to remove full rows` to describe the `_mark_lines` pipeline
   - Line 261: Change `// T-SPIN DETECTION (before _clear_lines, after piece written to board)` to `// T-SPIN DETECTION (before _mark_lines, after piece written to board)`
   - Line 392: Change `// All scoring happens here in _lock_piece after _clear_lines returns.` to `// All scoring happens here in _lock_piece after _mark_lines returns.`

4. Verify `_neon_row_mask` is still referenced (it should be -- `_mark_lines` uses it at lines 816-817). Do NOT remove it.

5. Build: run `make asm` to confirm no linker errors.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/yetris && make asm 2>&1 | tail -3 && echo "---" && grep -c "_clear_lines" asm/board.s && echo "(should be 0)"</automated>
  </verify>
  <done>
  - `_clear_lines` function completely removed from board.s (0 occurrences of the label)
  - File header accurately lists provided functions
  - All comments referencing `_clear_lines` updated to reference `_mark_lines`
  - `make asm` succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned data strings and audit .globl symbols</name>
  <files>asm/data.s</files>
  <action>
1. Remove the following orphaned string literals from data.s (these have zero code references and are not in any pointer table):
   - `_str_colon` (label + `.asciz ":"` + `.globl` directive) -- around line 436-437
   - `_str_paused_msg` (label + `.asciz "PAUSED"` + `.globl` directive) -- around line 456-457
   - `_str_press_p_resume` (label + `.asciz "Press p to resume"` + `.globl` directive) -- around line 458-459
   - `_str_title` (label + `.asciz "Y E T R I S"` + `.globl` directive) -- around line 494-495

   These strings were originally for pre-subwindow rendering and became orphaned when the pause overlay and menu were rewritten to use subwindows in v1.1.

2. Remove the `_clear_lines` comment reference in data.s:
   - Line 405: `// Used in _clear_lines to vectorize full-row check with ld1+uminv` -- update to: `// Used in _mark_lines to vectorize full-row check with ld1+uminv`

3. Audit: For every `.globl` symbol remaining in data.s, verify it has at least one reference outside data.s (either a direct `adrp`/`add` code reference or inclusion in a pointer table `.quad`). The following symbols should all survive (they are referenced):
   - All `_logo_lineN` symbols (referenced via `_logo_lines` pointer table, which is used by menu.s)
   - All `_str_help_*` symbols (referenced via `_help_lines` pointer table, which is used by menu.s)
   - All `_str_start`, `_str_quit`, etc. (referenced via `_menu_items` or `_settings_labels` pointer tables)
   - All game state variables (`_board`, `_piece_type`, `_score`, etc.)
   - All window pointers (`_win_*`)
   - All animation buffers and state (`_anim_*`)

4. Make individual `.globl` labels local (remove the `.globl` directive) for strings that are ONLY referenced through pointer tables in the SAME file (data.s) and never directly from other .s files. Specifically, the `_logo_lineN` symbols are only referenced via the `_logo_lines` table -- their individual `.globl` directives can be removed (keep the labels, just remove `.globl`). Similarly for individual `_str_help_*` strings accessed only via `_help_lines` table. And `_str_start`, `_str_help` (the menu item), `_str_quit` accessed via `_menu_items` table. And `_str_level_label`, `_str_ghost_label`, `_str_hold_label`, `_str_invis_label`, `_str_noise_label` accessed via `_settings_labels` table.

   NOTE: Keep the labels themselves (the linker needs them for the `.quad` references within the same file). Only remove the `.globl` directives for these table-only strings.

5. Build: run `make asm` to confirm clean build.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/yetris && make asm 2>&1 | tail -3 && echo "---" && grep -c "_str_colon\|_str_paused_msg\|_str_press_p_resume\|_str_title[^_]" asm/data.s && echo "(should be 0 -- orphaned strings removed)"</automated>
  </verify>
  <done>
  - 4 orphaned string literals removed from data.s
  - Comment referencing `_clear_lines` in data.s updated to `_mark_lines`
  - `.globl` directives removed for table-only string labels (labels retained for intra-file `.quad` references)
  - `make asm` succeeds with no errors
  - Every remaining `.globl` symbol in data.s has at least one cross-file reference or is a pointer table root
  </done>
</task>

</tasks>

<verification>
1. `make asm` succeeds — binary links and runs
2. `grep -c "_clear_lines" asm/board.s` returns 0
3. `grep -c "_str_colon\|_str_paused_msg\|_str_press_p_resume" asm/data.s` returns 0
4. Run `nm -g asm/bin/yetris-asm | grep -c "clear_lines"` returns 0 (not exported)
5. Run the game (`make asm-run`), play a few lines, confirm line clearing still works via the _mark_lines pipeline
</verification>

<success_criteria>
- Dead `_clear_lines` function (182 lines) removed from board.s
- 4 orphaned string literals removed from data.s
- Table-only `.globl` directives cleaned up (symbols made file-local)
- All comments updated to reflect the `_mark_lines` + `_clear_marked_lines` pipeline
- Binary builds and runs with no regression
</success_criteria>

<output>
After completion, create `.planning/phases/12-code-cleanup/12-01-SUMMARY.md`
</output>
